name: CI

# Fast feedback on every push and PR: typecheck + test + unicode lint.
# Ubuntu only, ~90 seconds. The heavy cross-platform compliance run lives in
# compliance.yml and only triggers on pull_request / workflow_dispatch.

on:
  push:
    branches: [main, master, develop]
  pull_request:
    branches: [main, master, develop]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    name: typecheck + test + lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6
        with:
          version: 9

      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # Build first: packages resolve cross-package types (e.g. @dcgp/core)
      # via each dependency's built dist/*.d.ts. On a fresh checkout dist/ is
      # gitignored and absent, so typechecking before building fails with
      # "Cannot find module '@dcgp/core'". pnpm -r builds in topological order.
      - name: Build all packages
        run: pnpm -r build

      - name: Typecheck all packages
        run: pnpm -r typecheck

      - name: Run test suite
        run: pnpm vitest run

      - name: Unicode lint (no em-dashes, no emojis in source or docs)
        run: node scripts/lint-unicode.mjs --dry
