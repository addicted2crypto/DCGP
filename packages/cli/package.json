{
  "name": "@dcgp/cli",
  "version": "1.0.0-rc.0",
  "type": "module",
  "description": "DCGP command-line tools: classify, status, paths, inject, gate, validate, init, entropy",
  "license": "MIT",
  "bin": {
    "dcgp": "./dist/cli.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@dcgp/core": "workspace:*",
    "@dcgp/paths": "workspace:*"
  }
}
